/* #include <stdio.h>
enum days{sun,mon,tue,wed,thu,fri,sat};
void main()
{

    enum days d = sat;
    printf("Number of the days is %d ",d+1);
} */

#include <stdio.h>
enum colors{red,green,blue};
void main()
{

    enum colors c=red;
    printf("Number of the days is %d ",c+1);
}
